package nl.vdzon.hkh.news.entity

/**
 * Ordinal order defines the required sort order (plek, persoon, gebeurtenis) for matched entities.
 */
enum class NewsEntityType {
    PLEK,
    PERSOON,
    GEBEURTENIS,
}
