apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: admin
spec:
  to:
    kind: Service
    name: admin
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
